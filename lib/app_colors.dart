import 'package:flutter/material.dart';

class AppColors {
  // Hlavné pozadie aplikácie (zladené s obrázkom Nesti)
  static const Color background = Color(0xFFF7F2EA);
  
  // Hlavná farba pre tlačidlá (jemná zelená)
  static const Color accent = Color(0xFFA8B49A);
  
  // Tu si môžeš v budúcnosti pridávať ďalšie farby
  static const Color primary = Colors.deepPurple;
  static const Color secondary = Color(0xFFE9D5BE);
}
