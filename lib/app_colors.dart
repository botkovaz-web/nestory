import 'package:flutter/material.dart';

class AppColors {
  // --- SVETLÝ REŽIM ---
  static const Color background = Color(0xFFF7F2EA);
  static const Color accent = Color(0xFFA8B49A);
  static const Color oliveDark = Color(0xFF6B7B5C);
  static const Color secondary = Color(0xFFE9D5BE);
  static const Color primary = Color(0xFF6B7B5C);
  static const Color terracotta = Color(0xFFD17A5D);

  // Jemnejšia biela pre karty (menej agresívna pre oči)
  static const Color surfaceLight = Color(0xFFFEFBF7); 

  // --- TMAVÝ REŽIM (Vlnené klbko / Teplý Antracit) ---
  static const Color backgroundDark = Color(0xFF1E1C1A);
  static const Color surfaceDark = Color(0xFF2A2624);
  static const Color textDark = Color(0xFFF2E9E1);
  static const Color accentDark = Color(0xFFD18D7A);
}
